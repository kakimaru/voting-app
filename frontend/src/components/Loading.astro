---
const {id} = Astro.props
---

<div class="loading" id={id}></div>

<style>
  .loading {
    margin: 4.8rem auto;
    width: 6.4rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(farthest-side, var(--color-green) 94%, transparent)
        top/10px 10px no-repeat,
      conic-gradient(transparent 30%, var(--color-green));
    -webkit-mask: radial-gradient(farthest-side, transparent calc(100% - 10px), black 0);
    animation: rotate 1.5s infinite linear;
  }

  @keyframes rotate {
    to {
      transform: rotate(1turn);
    }
  }
</style>